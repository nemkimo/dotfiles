#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '

export CHROME_EXECUTABLE=/bin/chromium
export PATH=$PATH:/home/eskimo/.cargo/bin

